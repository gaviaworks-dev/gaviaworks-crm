# tasks — defterler REPODA İZLENİR (plan, dersler, borç defteri, brief'ler, QA script'leri).
# Klonlayan biri projenin gerçek durumunu görebilmeli. Yalnız çıktı klasörleri hariç tutulur.
tasks/qa/screenshots/
tasks/qa/*.json
tasks/qa/*-out.txt

# QA çıktıları
docs/screenshots/
outputs/*.png
outputs/*.jpg

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Node (buildless proje ama Playwright QA için)
node_modules/
package-lock.json

# Claude local
.claude/settings.local.json
